[project]
name = "labflow"
version = "1.0.0"
description = "LabFlow - 实验室批次管理系统"
authors = ["SilenWang"]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tasks]
start = "python server.py"
dev = "python server.py"

[dependencies]
python = "*"
