# LabFlow 运行配置。systemd 服务与 `pixi run serve` 都读这份文件。
# 端口必须与 server/config.py 的默认值保持一致（9002），否则访问地址会对不上。

# 数据库后端：seekdb（生产）/ sqlite（回滚）
LABFLOW_DB=seekdb

# 监听端口
LABFLOW_PORT=9002

# 子路径部署前缀，留空表示挂在根路径
LABFLOW_BASE_PATH=

# 备份保留份数（backup.sh 轮转用）
LABFLOW_KEEP_BACKUPS=14
